{
  "configurations": [
    {
      "type": "chrome",
      "name": "Launch Chrome (needs Live Preview with root settings)",
      "request": "launch",
      "webRoot": "${workspaceFolder}",
      "url": "http://127.0.0.1:3000/"
    }
  ]
}